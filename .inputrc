
# display completion
set show-all-if-ambiguous on

# color when completing filename and dirs
set colored-stats on

# completion matches multiple items highlight the common matching prefix color
set colored-completion-prefix on

# ignore case
set completion-ignore-case on

# hifens and underscores same
set completion-map-case on

# append backslash to end of symlinked dirs
set mark-symlinked-directories on

# extra info when completing
set visible-stats on

# Show all autocomplete results at once
set page-completions off

# If there are more than 200 possible completions for a word, ask to show them all
set completion-query-items 200

# bash v4 completion
set skip-completed-text on

# no beep
set bell-style none

# Allow UTF-8 input and output, instead of showing stuff like $'\0123\0456'
set input-meta on
set output-meta on
set convert-meta off
